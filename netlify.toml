# Next.js 16 on Netlify uses the built-in runtime (no legacy plugin required).
# https://docs.netlify.com/integrations/frameworks/next-js/

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
