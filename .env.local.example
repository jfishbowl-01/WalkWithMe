# Copy to `.env.local` and fill in values. All are optional for local-first mode.

# Map basemap (recommended for production-quality styles)
# https://cloud.maptiler.com/
NEXT_PUBLIC_MAPTILER_KEY=

# Supabase (optional until auth/persistence is wired)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
